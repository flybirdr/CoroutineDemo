<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.rookie.coroutine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoroutineDemo">
        <activity android:name=".FlowActivity"></activity>
        <activity android:name=".ChannelActivity" />
        <activity android:name=".WithcontextActivity" />
        <activity android:name=".SupervisorscopeActivity" />
        <activity android:name=".SupervisorJobActivity" />
        <activity android:name=".CoroutineExceptionInChildActivity" />
        <activity android:name=".CoroutineExceptionInParentActivity" />
        <activity android:name=".CoroutineConcurrentActivity" />
        <activity android:name=".GlobalCoroutineActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>